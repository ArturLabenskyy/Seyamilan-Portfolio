import React from "react";

import Wrapped from "./navbar.styled";

const Navbar = () => {
    return (
        <Wrapped>
            <h3>SEYAMILAN Photography</h3>
        </Wrapped>
    );
};

export default Navbar;
